import { View, Text } from 'react-native'

export function StockOptions() {
  return (
    <View>
      <Text>
        Que onda perrito estas son las opciones que tenes en el stock:
        ACTUALIZAR VER TODAS, (vuelve a la pag anterior) BORRAR, CREAR una nueva
      </Text>
    </View>
  )
}
